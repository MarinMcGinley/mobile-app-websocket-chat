import { StyleSheet } from "react-native";
import { colors } from "../../../config/constants/colors";

export const styles = StyleSheet.create({
  messageContainer: {
    borderWidth: 2,
    borderColor: colors.beauBlue,
    backgroundColor: colors.white,
    marginBottom: 10,
    padding: 5,
    // flex: 1,
    // width: "inherit",
  },
});
