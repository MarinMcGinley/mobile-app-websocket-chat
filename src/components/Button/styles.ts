import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonContainer: {
    height: 50,
    width: 250,
    marginTop: 5,
    marginBottom: 5,
    borderWidth: 1,
    borderRadius: 5,
  },
  pressableView: {
    flex: 1,
  },
  textContainer: {
    flex: 1,
    justifyContent: "center",
  },
  text: {
    textAlign: "center",
    fontWeight: "500",
  },
});
